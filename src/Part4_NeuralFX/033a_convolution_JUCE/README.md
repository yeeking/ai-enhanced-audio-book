# plugin that implements convolution

Minimal plugin that loads an impulse response from disk then uses it with the JUCE dsp convolution system
to convolve an incoming signal with the impulse response.
